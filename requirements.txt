# CLI Interface construction
typer>=0.9.0

# Pretty terminal output (tables, panels, markdown)
rich>=13.0.0

# Data manipulation and export (CSV, SQL)
pandas>=2.0.0

# System resource monitoring (RAM/CPU tracking)
psutil>=5.9.0

# HTML Dashboard generation (templating)
jinja2>=3.1.0

# Bioinformatics file handling (BAM/SAM/CRAM stats)
# Note: pysam requires htslib installed on the system (usually handled by pip/conda)
pysam>=0.21.0